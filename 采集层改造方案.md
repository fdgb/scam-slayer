# Scam-Slayer 采集层改造方案（诚实现实版）

> **原始目标**：净归档案例（仅 🔴高危 + 🟡存疑 写入 `case-library`）达到 **1000–2000 条/天**
> **当前结论（2026-08-08 实测）**：⚠️ **靠现有可用源做不到**。诚实可达区间见第 5 节。
> 状态：方案评审稿（代码已部分落地）｜起草 2026-08-08，修订 2026-08-08（Bilibili 已确认放弃 + 科学辟谣已修好）

---

## 0. 目标与口径（确认）

- **净归档案例** = 规则引擎 `auto_verdict.py` 只把 `🔴高危` / `🟡存疑` 写进 `case-library.md`，`🟢可信` 不归档。
- **已辟谣库**（`rumor-library.md`）= 官方辟谣平台的「真相文本」（🟢），与诈骗案例库**严格分离**，用于增强 `truth-base.json` 与对照，不参与净归档计数。
- 鉴别端（规则引擎，不调 LLM、零 token）**不是瓶颈**。瓶颈 100% 在**采集量**与**可用高体量源**。

---

## 1. 现状诊断（沙箱 + 生产实测 · 2026-08-08）

| 采集源 | 代码位置 | 实测结果 | 结论 |
|---|---|---|---|
| B站搜索 | `trainer/collect_bilibili.py` | API 返回 **HTML 错误页（风控拦截，非 WBI 问题）**；nav 接口可通但 search/type 在 WBI 之前就被 IP 层挡掉，带 cookie/代理均无解 | ❌ **已确认放弃**（沙箱/本机 IP 风控死结） |
| 微信（搜狗）| `trainer/collect_wechat_weibo.py` | 沙箱探活返回验证码页；**生产自动化实际 +112/天、累计 4567**（UA/IP 差异）→ 生产可用 | ✅ 生产可用，现状**唯一** 🔴/🟡 主力源 |
| 微博 | 同上 `m.weibo.cn` | 需 cookie，无 cookie 恒为 0 | ❌ 暂废（需 cookie） |
| 海外 | `trainer/collect_overseas.py` | `get_mock_overseas_data()` 返回「【模拟数据】」，**100% 假数据** | ❌ 造假，已标记删除 |
| ✅ 联合辟谣平台 | `trainer/collect_piyao.py` | 81 条历史（首页 + 已有；`index_N.html` 分页 404，仅抓到存量） | ✅ 可用·权威真相库 |
| ✅ 科学辟谣 | `trainer/collect_piyao.py` | **本轮刚修好**：71 条历史（8 页，`rumordetail?id=` 结构） | ✅ 可用·权威真相库 |
| ⚠️ 黑猫投诉 | tousu.sina.com.cn | 200 但 JS 渲染，需破解 JSON 列表接口；含第三方隐私须脱敏 | ⚠️ 高体量可选（Phase 3） |

**一句话**：四个原始实时源 = 一个生产可用（Sogou）+ 三个废（B站风控/微博无cookie/海外造假）。"一天几条"的根因是**没有高体量源**——Bilibili 本是最优解，但被风控卡死。

---

## 2. 核心瓶颈（4 个，已重排）

1. **高体量源缺失（死穴）**：B站被风控拦截，现有可用源里只有 Sogou 能产 🔴/🟡，自然天花板就是"几十/天"。这是距目标差 1–2 个数量级的唯一原因。
2. **反爬**：微博移动端无 cookie 恒空；搜狗微信生产可用但鲁棒性弱（依赖 UA/IP），沙箱会被验证码挡。
3. **海外模块造假**：`collect_overseas.py` 整个是 mock，对真实训练零贡献。
4. **去重 + 频次错配**：永久去重 + `order=click` + 一天一次 → 跑几天新增归零。

---

## 3. 源策略（务实版，按 ROI 重排）

### 3.1 已落地的「能跑源」（保留 + 扩大频次）
- **搜狗微信（Sogou）**：现状主力。生产 +112/天原始。→ 调度改**每小时**跑（`0 * * * *`），靠时间窗去重抓新帖，可把原始量从 ~112/天 提到 ~150–200/天（受 Sogou 反爬上限约束）。
- **联合辟谣平台 + 科学辟谣**：已接入 `collect_piyao.py`，本轮实测 81 + 71 = **152 条历史**进 `rumor-library.md`。日新增靠官方发稿（~5–30/天），价值在"权威真实谣言文本"补 `truth-base`，**不是冲量主力**。

### 3.2 Bilibili —— 暂放弃（沙箱无解），留生产环境后路
- 已验证：WBI 签名算法本身正确（mixin_key 计算无误），但 `search/type` 在 WBI 之前就被 IP 风控拦截返回 HTML 错误页。带 `buvid3` cookie、走本地代理均无效。
- **唯一复活路径**：在**有干净出口 IP 的生产服务器**上跑 + 带登录态 cookie + 完整 WBI。本沙箱/本机不可行。→ 列为「待你提供生产环境」的 Phase 4 选项，不阻塞当前。

### 3.3 黑猫投诉 / 抖音 —— 高体量可选（Phase 3，想冲量必做其一）
- **黑猫投诉**：数千投诉/天，体量足够把原始量拉到几千。需破解其 JSON 列表接口（非裸 HTML）+ **入库前强制脱敏**（去姓名/手机号/订单号，守既定脱敏铁律）。注意：投诉 ≠ 诈骗案例，需口径适配或单独建「投诉特征库」。
- **抖音**：体量最大，但需官方/半官方接口或逆向，成本高、稳定性差，优先级最低。

### 3.4 砍掉 / 标记
- **微博**：无 cookie 恒为 0，从采集器移除或改为"有 cookie 才启"。
- **海外 mock**：删除 `get_mock_overseas_data` 造假逻辑；如需真实海外数据，改接 YouTube Data API（需 key + 配额），留待 Phase 3。

---

## 4. 架构改造清单（已落地 + 待做）

| 项 | 状态 | 说明 |
|---|---|---|
| `collect_piyao.py` | ✅ **已重写** | 联合辟谣 + 科学辟谣双源；科学辟谣 `rumordetail?id=` 解析已修，本轮 +71 |
| `collect_wechat_weibo.py` 关键词 | ✅ **已扩** | 14 → ~35 个（养生/投资/AI/情感/诈骗），Sogou 主力源覆盖加宽 |
| `run_trainer.py` | ✅ **已接** | 采集阶段已调用 `collect_piyao.main()` |
| `rumor-library.md` | ✅ **已建** | 152 条（81+71），与 case-library 分离 |
| `collect_bilibili.py` | ⏸ **暂停** | 风控死结，留生产环境后路；不阻塞 |
| `collect_overseas.py` | ⏳ 待删 mock | 删造假逻辑，或接 YouTube API |
| 去重策略 | ⏳ 待改 | "永久去重" → "时间窗去重"（30 天内见过才跳）+ `pubdate` 抓新 |
| 调度频次 | ⏳ 待改 | 每日 1 次 → 每小时 1 次（`0 * * * *`） |
| `auto_verdict.py` | 不动 | 规则引擎已零成本，直接吃新增量 |
| 监控 | ⏳ 待加 | 每次运行输出各源新增/失败计数，B站/搜狗异常告警 |

---

## 5. 量级测算（诚实区间 · 2026-08-08 修订）

| 场景 | 原始采集/天(生产) | 净归档(🔴+🟡)/天 | 达标？ |
|---|---|---|---|
| 现状（Sogou生产 + piyao/kepuchina 每日1次）| ~120–180 | **~25–50** | ❌ 差 1–2 数量级 |
| 改每小时跑（Sogou更频 + piyao每时）| ~200–400 | ~30–60 | ❌ 仍差数量级 |
| + 黑猫投诉（破解+脱敏）| ~2000–5000 | ~200–800 | ⚠️ 接近但未破千 |
| + Bilibili 生产部署（干净IP+登录态+WBI）| ~3000–8000 | **~600–2500** | ✅ 唯一达标路径 |
| 现状（不改造·沙箱）| ~0–5 | ~0 | ❌ |

**结论（诚实）**：
- 不解决「高体量源」问题，**净归档永远在几十/天**，与原始目标 1000–2000 差 1–2 个数量级。
- 唯一能冲千条级的路径 = **Bilibili 生产环境部署**（需你提供干净 IP 服务器）；退而求其次 = **黑猫投诉**（可把量拉到几百/天，但属"投诉"非"诈骗案例"，需口径适配）。
- 科学辟谣/联合辟谣的价值在**质**（权威真相基准），不在量。

---

## 6. 风险与合规

- **反爬 / IP 封禁**：Sogou 每小时跑需限速（每请求间隔 2–3s、避免单 IP 暴击）；Bilibili 复活必须在干净 IP 环境。
- **第三方内容隐私**：黑猫投诉等含个人信息，入库前强制脱敏，守既定脱敏铁律。
- **数据质量**：量大后规则引擎可能漏判/误判，建议每周抽 50 条人工核对。
- **真实性边界**：归档案例标注"机器初判"，不宣称人工核实，守诚实红线。

---

## 7. 分阶段交付（务实版）

- **Phase 1（已做）**：重写 `collect_piyao.py`（双源，科学辟谣已修）→ 已辟谣库 152 条种子；扩 Sogou 关键词；`run_trainer.py` 接入。
- **Phase 2（待做·低风险）**：去重改时间窗 + 调度改每小时 + `generate_report` 纳入 piyao/已辟谣库统计 + 监控告警。
- **Phase 3（可选·冲量）**：黑猫投诉（破解+脱敏）/ 海外接 YouTube API。
- **Phase 4（达标必经·需你提供资源）**：Bilibili 生产环境部署（干净 IP + 登录态 cookie + WBI）。

---

## 8. 待你决策（路线选择）

> 原始目标 1000–2000 净归档/天，靠"接稳能跑的源"达不到（上限几十/天）。要达标必须投入攻坚高体量源。请选方向：

- **A. 接受现实，重质不重量**：已辟谣库 152 种子 + 诈骗案例库几十/天（Sogou 驱动）。精力转向「质量 + 多端分发(SkillHub/Coze/小程序) + 变现」，不堆量。
- **B. 攻坚黑猫投诉（高体量+脱敏）**：把原始量拉到几千/天，净归档有望几百/天（非千条级，且属"投诉"口径）。
- **C. 复活 Bilibili（需你提供干净 IP 生产服务器）**：唯一能冲千条级的路径，但依赖你提供可部署环境 + 登录态 cookie。
